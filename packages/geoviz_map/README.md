# geoviz-map

QPainter-based geographic map visualization for PySide6. Web Mercator projection, layer-based rendering, offline-friendly. Suitable for embedding into desktop apps that need to plot points and polygons on a map without WebEngine.

Part of the geo-viz-engine workspace.
